#include <bits/stdc++.h>
using namespace std;

int main(){
    int n; cin >> n;
    vector <int> v;
    for(int i = 0; i < n; i++){
        int a; cin >> a;
        v.push_back(a);
    }
    
    vector <int> :: iterator it;
    sort(v.begin(), v.end());
    do {
        for(it = v.begin(); it != v.end(); it++){

    }
    while(next_permutation(v.begin(), v.end()));
}