#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m, f, i = 0;
    cin >> n >> m >> f;
    int a[i];

    if(f == 1){

        while(n <= a[i] && a[i] <= m){
            i++;
            cout << (pow(a[i], 2));
        }
    }

    else if(f == -1){
        
        sort(a, a + 1000)
        for(int i = 0; i < )
    }
}